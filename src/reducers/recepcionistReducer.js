import {types} from '../types/types';

const initial = {

}

export const recepcionistReducer = (state = initial, action) => {
    switch (action.type) {
        default:
            return state;
    }
}